{
  "name": "files-array",
  "version": "2.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node index.js",
    "dev:app": "cd app && yarn dev",
    "build": "cd app && yarn build",
    "dev": "nodemon index.js",
    "emulator": "firebase emulators:start",
    "deploy": "firebase deploy",
    "create-account": "near create-account app.${USER_NEAR}.testnet --masterAccount ${USER_NEAR}.testnet"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "firebase": "^9.0.2",
    "firebase-admin": "^9.11.1"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "prettier": "^2.4.1"
  }
}
